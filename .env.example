# Copy this file as .env and fill in the values.
# .env is listed in .gitignore: it is never committed to the repository.

# Number of rows emitted per iteration
CHUNK_SIZE=1000

# Local path to the input file (kept outside the repository)
INPUT_FILE=./data/input.xlsx

# n8n webhook URL that receives the batches
N8N_WEBHOOK_URL=
